# Normalise line endings to LF in the repo (this deploys to Linux).
* text=auto eol=lf

# Binary assets (if added later)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
